{% set substitutions = {
    "💥": "Breaking Change",
    "✨": "Feature",
    "🚑️": "Hotfix",
    "🔒️": "Security",
    "🐛": "Fix",
    "⚡️": "Performance",
} %}
## What's Changed
{% for type_, commits in release["elements"] | dictsort | reverse %}
### {{ substitutions.get(type_, type_) }}
{%- if type_ != "unknown" %}
{% for commit in commits %}
* {{ commit.descriptions[0] }} by {{ commit.commit.author.name }} in [`{{ commit.short_hash }}`]({{ commit.hexsha | commit_hash_url }})
{%- endfor %}{% endif %}{% endfor %}
